---
layout: base.njk
---

<div class="pt-12 pb-16 hero-box">
    <div class="flex flex-wrap w-full max-w-xl text-sm text-center mx-auto pb-2">
        {% for item in portfolioSing.types %}
            <div class="italic mx-auto pr-4">
                {{ item.name }}{% if loop.last !== true %} {% endif %}
            </div>
        {% endfor %}
    </div>
    <h1 class="max-w-7xl font-new text-2xl sm:text-4xl xl:text-5xl xxl:text-6xl text-center leading-tight mx-auto px-4">{{ portfolioSing.title | safe }}</h1>
    <div class="max-w-3xl text-center mx-auto px-4 opacity-75 whitespace-pre-line">{% if portfolioSing.aboutText %}{{ portfolioSing.aboutText }}{% else %}More about this project coming soon...{% endif %}</div>
</div>

<div class="flex flex-wrap max-w-12xl mx-auto portfolio-gallery">
{% if portfolioSing.gallery %}
{% for item in portfolioSing.gallery %}
    <style>
        .portfolio-{{ item.id }} {
            background-image: url('{{ item.url }}');
        }
    </style>
    <a href="{{ item.url }}" class="relative w-full sm:w-1/2 lg:w-1/3 bg-cover bg-center border-4 border-white mx-auto py-32 md:py-40 xl:py-64 portfolio-item portfolio-{{ item.id }}">
        
    </a>
{% endfor %}
{% else %}
<h6 class="w-full font-new font-bold text-center pt-12">Check back later for more photos of this project.</h6>
<div class="py-80"></div>
{% endif %}
</div>